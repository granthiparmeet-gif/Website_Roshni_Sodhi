services:
  web:
    build:
      context: ./web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./web:/app
      - /app/node_modules
    command: npm run dev -- --hostname 0.0.0.0 --port 3000







